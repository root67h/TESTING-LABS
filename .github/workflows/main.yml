name: RDPburp

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest

    steps:
      # -------------------------------
      # Create RDP User (Fixed Password)
      # -------------------------------
      - name: Setup RDP User
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          $user = "runneradmin"
          $pass = "Str@ngeRDP#2026"   # 🔒 Fixed password

          net user $user $pass
          net localgroup administrators $user /add

          echo "RDP_USER=$user" >> $env:GITHUB_ENV
          echo "RDP_PASS=$pass" >> $env:GITHUB_ENV

      # -------------------------------
      # Enable RDP
      # -------------------------------
      - name: Enable RDP
        shell: pwsh
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
            -Name "fDenyTSConnections" -Value 0

          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      # -------------------------------
      # Install Burp Suite
      # -------------------------------
      - name: Install Burp Suite
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          $url = "https://portswigger.net/burp/releases/download?product=community&version=latest&type=WindowsX64"
          $exe = "$env:TEMP\burpsuite_installer.exe"

          Write-Host "Downloading Burp Suite..."
          Invoke-WebRequest -Uri $url -OutFile $exe

          Write-Host "Installing..."
          Start-Process -FilePath $exe -ArgumentList "-q" -Wait

          $target = "C:\Program Files\BurpSuiteCommunity\BurpSuiteCommunity.exe"

          if (!(Test-Path $target)) {
            $found = Get-ChildItem "C:\Program Files\" -Recurse -Filter "BurpSuiteCommunity.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
            if ($found) {
              $target = $found.FullName
            } else {
              Write-Host "ERROR: Burp not found"
              exit 1
            }
          }

          # Create Desktop Shortcut
          $desktop = "C:\Users\$env:USERNAME\Desktop"
          $wsh = New-Object -ComObject WScript.Shell
          $shortcut = $wsh.CreateShortcut("$desktop\Burp Suite.lnk")
          $shortcut.TargetPath = $target
          $shortcut.WorkingDirectory = Split-Path $target
          $shortcut.Save()

      # -------------------------------
      # Install Tailscale
      # -------------------------------
      - name: Install Tailscale
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $path = "$env:TEMP\tailscale.msi"

          Write-Host "Downloading Tailscale..."
          Invoke-WebRequest -Uri $url -OutFile $path

          Write-Host "Installing..."
          Start-Process msiexec.exe -ArgumentList "/i `"$path`" /quiet /norestart" -Wait

      # -------------------------------
      # Connect Tailscale
      # -------------------------------
      - name: Connect Tailscale
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"

          $authKey = "${{ secrets.TAILSCALE_AUTH_KEY }}"

          if ([string]::IsNullOrEmpty($authKey)) {
            Write-Host "ERROR: Missing Tailscale auth key"
            exit 1
          }

          Write-Host "Connecting..."
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
            --auth-key="$authKey" `
            --hostname="gh-rdp-$env:GITHUB_RUN_ID" `
            --accept-routes

          Start-Sleep 10

          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4

          if (-not $ip) {
            Write-Host "ERROR: No IP assigned"
            exit 1
          }

          echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV

      # -------------------------------
      # Show Details + Keep Alive
      # -------------------------------
      - name: Show RDP Details
        shell: pwsh
        run: |
          Write-Host "==========================="
          Write-Host "RDP IP   : $env:TAILSCALE_IP"
          Write-Host "Username : $env:RDP_USER"
          Write-Host "Password : $env:RDP_PASS"
          Write-Host "==========================="

          # Keep session alive
          while ($true) {
            Start-Sleep 300
          }
